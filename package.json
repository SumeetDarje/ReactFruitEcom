{
  "name": "json-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 10000",
     "dev": "vite",
  "build": "vite build",
  "preview": "vite preview"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
